name: ehlphabet

on:
  schedule:
    - cron:  '0 * * * *'

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2.3.3

    - name: mirror
      run: ./mirror.sh
      env:
        source_repo: https://git.bananach.space/ehlphabet.git
        github_repo: ehlphabet.git
        branch: master
        access_token: ${{ secrets.ACCESS_TOKEN }}
